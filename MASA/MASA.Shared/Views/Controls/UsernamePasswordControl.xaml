<UserControl
    x:Class="MASA.Views.Controls.UsernamePasswordControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MASA.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="180"
    d:DesignWidth="400"
    DataContext="{Binding RelativeSource={RelativeSource Self}, Path=UsernamePasswordControlViewModel, Mode=TwoWay}"
    >

    <Grid Margin="0,10,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0" Header="Username" Text="{Binding Username, Mode=TwoWay}"/>
        <PasswordBox Grid.Row="1" Header="Password" Password="{Binding Password, Mode=TwoWay}" />
    </Grid>
    
    
</UserControl>
